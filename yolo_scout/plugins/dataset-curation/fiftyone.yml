name: "@ultralytics/dataset-curation"
version: "1.0.5"
description: "Deduplicate near-duplicates while preserving quality variation, and export the current view as a YOLO dataset"
fiftyone_compatibility: ">=1.0"
operators:
  - select_diverse_subset
  - export_kept_view
